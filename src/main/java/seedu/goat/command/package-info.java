/**
 * The set of instructions GOAT understands.
 * <p>
 * {@link seedu.goat.command.Command} lists one constant per keyword the user can type,
 * so the compiler knows every possible command and the help text can be generated from
 * the constants rather than maintained separately.
 */
package seedu.goat.command;
