/**
 * Everything the user sees and types.
 * <p>
 * Collecting the input stream and the output formatting in {@link seedu.goat.ui.Ui}
 * means the rest of GOAT never writes to standard output directly, so the wording and
 * the layout can change without touching command logic.
 */
package seedu.goat.ui;
