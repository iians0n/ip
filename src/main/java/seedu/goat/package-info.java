/**
 * GOAT, a chatbot that tracks a list of tasks.
 * <p>
 * {@link seedu.goat.Goat} wires the parts together and holds the command handlers, and
 * {@link seedu.goat.Launcher} opens the window that the packaged application starts in.
 * The two classes kept here rather than in a subpackage are used by every layer:
 * {@link seedu.goat.GoatException}, which carries a message written for the user, and
 * {@link seedu.goat.DateFormats}, which converts between the stored and displayed forms
 * of a date.
 */
package seedu.goat;
